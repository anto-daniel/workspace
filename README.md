# workspace
Personal workspace

Adding some useful scripts which comes in handy during development

The scripts are located in the `scripts` directory. You can run them using the command line or terminal.
